name: aqr_app
description: 'App for generating and scanning advanced QR codes.'
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  image: ^4.0.17
  aqr_lib:
    path: ../aqr_lib

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
