name: aqr_app
description: 'App for generating and scanning advanced QR codes.'
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  image: ^4.2.0
  wakelock_plus: ^1.2.10
  zxing_lib:
    path: /home/enery/Documents/aqr/aqr_app/aqr_lib

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
