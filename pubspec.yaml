name: aqr_app
description: 'App for generating and scanning advanced QR codes.'
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  image: ^4.0.17
  wakelock_plus: ^1.2.10
  aqr_lib:
    path: ../aqr_lib
  file_picker: ^10.1.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
